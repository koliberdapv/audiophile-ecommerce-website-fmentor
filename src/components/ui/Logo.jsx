import logo from '../../assets/shared/mobile/logo.svg';

const Logo = () => {
	return (
		<figure className="logo | grid center">
			<img
				src={logo}
				alt="audiophile"
			/>
		</figure>
	);
};

export default Logo;
