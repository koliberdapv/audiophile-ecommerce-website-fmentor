import { Link } from 'react-router-dom';
import { Cart } from '../../components/ui';

const Checkout = () => {
  return (
    <div>
      <h1>checkout</h1>
      <Cart />
    </div>
  );
};

export default Checkout;
